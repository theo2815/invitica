/**
 * Name of the meta element the Worker writes at serve time to hand the client-exposed,
 * domain-restricted MapTiler key to the hydrated page (ADR-006). Published snapshots stay keyless.
 */
export const MAP_TILE_KEY_META = "invitica-map-tile-key";
